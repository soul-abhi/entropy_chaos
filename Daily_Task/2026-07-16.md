# Daily Task Log — 2026-07-16

## Session Summary

Work done on the Adaptive Risk-Aware Chaos Engineering Framework during this chat session.

## What We Did

1. **Created `score.md` (project assessment)**
   - Rated the project IDEA: **7.5/10** (real problem, good scope, research hook; risk-gated chaos not novel, "adaptive" not yet earned).
   - Rated current CONDITION: **5.5/10** (sound architecture, working pipeline, but core behavior inert and no tests).
   - Set TARGET: **9/10** with a roadmap.
   - Added Research Paper Theory section (foundational reading, how to check existing papers, paper structure, contribution statement).
   - Added Netflix Chaos Monkey comparison (side-by-side table + positioning).
   - Key finding: the `SAFE` state is mathematically unreachable at default constants, so the auto pod-delete never fires (FLAGGED-01).

2. **Consolidated documentation into `doc/`**
   - Created `doc/setup.md` (full Windows + Linux setup, teardown, troubleshooting, Mermaid diagram).
   - Created `doc/project_timeline.md` (element tracker, done/doing/will-do, Flagged Issues with root cause + fix + future note, and a 9/10 genuine-impact tracker).
   - Moved existing docs (PROJECT_DESCRIPTION, report, score, START_FROM_MINIKUBE, chaos-module) into `doc/`.
   - Removed scattered root markdown files.

3. **Rebuilt root `README.md` as an attractive showcase**
   - Color via shields.io status badges, Mermaid architecture + decision diagrams, feature/status tables, doc index, Chaos Monkey comparison.
   - Honored the standing no-emoji rule (color comes from badges/diagrams, not emoji).

4. **Added `doc/` to `.gitignore`** (local-only docs).

5. **Restored 4 deleted docs**
   - User deleted `PROJECT_DESCRIPTION.md`, `report.md`, `START_FROM_MINIKUBE.md`, `chaos-module.md` from `doc/`.
   - Recovered them byte-exact from commit `8562886` (sizes verified: 12747 / 5726 / 2772 / 270).

6. **Trimmed `doc/` to remove redundancy**
   - Removed `START_FROM_MINIKUBE.md` (covered by `doc/setup.md`), `report.md` (superseded by `doc/score.md`), `chaos-module.md` (duplicated in `doc/setup.md` Section 7).
   - Updated README documentation table to avoid broken links.
   - `doc/` now holds: `setup.md`, `project_timeline.md`, `score.md`, `PROJECT_DESCRIPTION.md`.

7. **Created `Daily_Task/` folder** for end-of-chat dated logs (this file).

## Open Items / Next Steps

- Fix FLAGGED-01 (SAFE-unreachable) so adaptive chaos actually triggers.
- Add unit + integration tests.
- Make Service B a real dependency of Service A (cascade demo).
- Add a second fault primitive and persistence; write the research paper.

## Notes

- `doc/` and can be gitignored; `Daily_Task/` currently tracked — confirm if it should also be local-only.
- No commit was made this session (user controls commits).
